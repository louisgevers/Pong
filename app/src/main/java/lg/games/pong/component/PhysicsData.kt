package lg.games.pong.component

/**
 * Represents point coordinates in a 2D space.
 */
data class Point(var x: Int, var y: Int)

/**
 * Represents a vector direction in a 2D space.
 */
data class Vector(var dx: Int, var dy: Int)